# 再帰・分割統治（Divide and Conquer）

## 問題1: 繰り返し二乗法

整数 `a`, `b`, `mod` が与えられる。`a^b mod mod` を求めよ。

### 入力例
```
a = 2, b = 10, mod = 1000
```

### 出力例
```
24
```

---

## 問題2: 転倒数

配列 `A` の転倒数（`i < j` かつ `A[i] > A[j]` となるペアの数）を求めよ。

### 入力例
```
A = [3, 1, 2]
```

### 出力例
```
2
```
