# ソート（Sorting）

## 問題1: K番目に大きい要素

整数配列 `nums` と整数 `K` が与えられる。K 番目に大きい要素を求めよ。

### 入力例
```
nums = [3, 2, 1, 5, 6, 4]
K = 2
```

### 出力例
```
5
```

---

## 問題2: 区間の重なり判定

区間のリスト `intervals` が与えられる。重なりがあるか判定せよ。

### 入力例
```
intervals = [[1, 3], [2, 5], [6, 8]]
```

### 出力例
```
true
```
